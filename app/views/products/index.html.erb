<% cart.each do |item| %>
    <%= item %><br>
<% end %>

<%= form_tag(action: "add", method: "post") do %>
    <input type="text" name="product">
    <input type="submit" value="add to cart">
<% end %>